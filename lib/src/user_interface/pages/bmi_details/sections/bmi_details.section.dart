import '../../_section.imports.dart';
import '../views/_.index.dart';

class BmiDetailsSection extends StatelessWidget {
  const BmiDetailsSection();

  @override
  Widget build(BuildContext context) {
    return BmiDetailsView();
  }
}
